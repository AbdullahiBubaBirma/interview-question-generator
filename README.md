# AI-Powered Interview Question Generator

A simple React application that generates 3 thoughtful interview questions for any given job title using the **Google Gemini API**.  
This project was built as part of a technical screen for a Founding Engineer role.

---

## 🚀 Features
- Enter any job title (e.g., "Customer Success Manager").
- Calls the Gemini `gemini-pro` model to generate tailored interview questions.
- Clean, minimal UI with loading and error states.
- Secure API key handling via environment variables.

---

## 🛠️ Tech Stack
- React (Create React App)
- Google Gemini API (`gemini-pro` model)
- Hosted on Netlify/Vercel

---

## 📂 Project Structure

Saturday, May 9
About the job
Technical Co-Founder or Founding Engineer



Location: Chicago or open (Global)

Full-Time

Remote



About the Opportunity

Our client is an early-stage HRTech startup building an AI-powered product. They have a clear product vision, a validated prototype, early positive feedback from potential customers and a growing advisor team. They are looking for a Technical Co-Founder or Founding Engineer to help bring this vision to life and build something meaningful from the ground up.



The founder is non-technical, and this role is a true leadership position and partnership. The right person will not just write code. They will help shape the product, the architecture, and the direction of the company alongside the founder. This is a ground-floor opportunity for someone who wants to build, lead, and own something from day one.



What Our Client Is Looking For

Our client believes curiosity and the ability to learn are more important than years of experience. The right person moves fast, picks up new technologies quickly, takes real ownership, and genuinely wants to help shape what this product becomes. This is a leadership role and a partnership as much as it is a technical position.



More specifically, they are looking for someone who:

Has a strong technical foundation and has built real things
Has experience or genuine interest working with AI and LLM APIs
Is curious by nature and sees learning as a core part of the job
Can make good decisions with incomplete information and iterate quickly
Communicates clearly and can work closely with a non-technical founder without getting lost in jargon
Takes initiative and ownership rather than waiting to be told what to build
Is energized by early-stage ambiguity, not slowed down by it




Non-Negotiables

Full-time availability. This is not a freelance or part-time engagement.
Professional fluency in English, written and verbal. You will be working daily with a US based founder and US based customers.
Minimum 4 hours of daily overlap with Central Time (US)
Ability to meet regularly via Zoom with the founding team


Compensation

Compensation is structured around the type of partnership and will be discussed directly with each candidate. Depending on the arrangement, this could be a combination of equity and base salary, or equity only. Our client believes in building something together and compensating the right person accordingly.



The Process

30-minute technical assignment — a focused async build submitted with a short Loom video walkthrough
Intro call with the Founder — for candidates who pass the technical screen
Final technical assessment — a larger build
Conversation with Founder and Advisor
Co-working day with Founder for finalists
Reference check
Offer


Ready to be considered? Start here.

If this opportunity excites you, here is your next step. Complete Step 1 now and include it with your application.



Overview

This is a short, focused technical screen designed to take no more than 30 minutes. It is not about perfection. It is about how you think, how you work, and how you communicate your decisions.

There is no right approach. What matters is that it works, that you can explain it, and that you followed the requirements.



What to Build

Build a simple web page that does the following:

Accept a job title as a text input (use Customer Success Manager as the primary example)
On submission, call an AI API and return 3 thoughtful interview questions specific to that role


Requirements

The page must be functional and the API call must work
Use an AI API provider of your choice. Gemini offers a generous free tier at ai.google.dev with no credit card required. Additional free options are listed at https://github.com/cheahjs/free-llm-api-resources. Paid tiers are acceptable if you choose to use them at your own cost.
Model selection is up to you — pick what you know or what makes sense for the task
Privacy and Security: Do not include any personal information such as names, phone numbers, or private resumes in your prompts or code. Stick to generic job titles
The UI does not need to be polished, but it should be clean and readable. Please include a basic loading state while the API is fetching the questions
Write code as if someone else will read it — clarity and simple error handling matter
Submit your code via a Github link and host your page live using a free hosting provider. Include the live URL in your submission so we can interact with your build directly


The Loom Video

Record a short Loom video of 4 to 7 minutes. In the video:

Introduce yourself
Show the working application in real-time.
Walk through your code and explain the key decisions you made, including the prompt you used. Please ensure to answer: What provider and model did you choose and why?
Answer the following questions:

Tell us one thing you would do differently or improve if you had more time.
What is your philosophy around building in general?
How do you collaborate with others when building?
How do you figure things out when you are stuck?


This video matters as much as the code. The ability to explain your thinking clearly to a non-technical person is a core part of this role.



How to Submit

Email to: swati@meloassociates.com
Subject line: Technical Screen — [Your Name]
Include: Assignment and Loom video
Deadline: May 25, 2026 - Nothing will be reviewed before this deadline.


A Note on Using AI

Our client is building an AI-powered product and working fluently with AI tools is part of the job. Do not hide that you used AI. Show us how you used it—whether for code generation, debugging, or refining your prompt.



Our client is an Equal Opportunity Employer. All qualified applicants will receive consideration for employment without regard to race, color, religion, sex, sexual orientation, gender identity, national origin, disability, or veteran status.
This role is a classic “ground-floor” opportunity: you’d be stepping in as the technical backbone of an early-stage HRTech startup, working alongside a non-technical founder to shape both the product and the company itself. Here are the key takeaways from the posting you’re reviewing:

🌟 The Opportunity
Early-stage HRTech startup with a validated prototype and positive early feedback.

Founder is non-technical, so this role is both leadership + partnership.

You’d be responsible not just for coding, but for architecture, product direction, and technical vision.

🧑‍💻 What They’re Looking For
Strong technical foundation with real-world builds.

Interest or experience with AI and LLM APIs.

Curiosity and fast learning as core traits.

Comfort with ambiguity and making decisions with incomplete info.

Clear communication with non-technical partners.

Initiative and ownership — someone who thrives in early-stage chaos.

🚫 Non-Negotiables
Full-time commitment (not freelance/part-time).

Fluent English (written + verbal).

At least 4 hours overlap with US Central Time.

Regular Zoom collaboration with the founder.

💰 Compensation
Flexible: equity + salary, or equity-only depending on arrangement.

Emphasis on shared ownership and building together.

🛠️ The Process
30-min technical assignment (async build + Loom walkthrough).

Intro call with founder.

Larger technical assessment.

Conversation with founder + advisor.

Co-working day with founder.

Reference check.

Offer.

📌 The Technical Screen (Step 1)
Build a simple web page:

Input: job title (e.g., “Customer Success Manager”).

On submit: call an AI API → return 3 tailored interview questions.

Requirements:

Functional API call.

Use any AI API provider (Gemini suggested).

Clean, readable UI with loading state.

Clear code with error handling.

Host live + share GitHub link.

Loom video (4–7 min):

Introduce yourself.

Show app working.

Walk through code + decisions (provider/model choice).

Answer reflection questions (improvements, philosophy, collaboration, problem-solving).

👉 If you’re considering applying, the most important part is showing how you think and communicate — not just the code. They want someone who can explain technical decisions clearly to non-technical partners and thrive in ambiguity.

Would you like me to sketch out a starter code example for that technical screen (using, say, Gemini API + a simple React or vanilla JS frontend), so you can see how to structure it?

Yes
Perfect — let’s sketch out a starter implementation for the technical screen. I’ll keep it simple, clean, and easy to extend. We’ll use vanilla HTML + JavaScript with the Google Gemini API (since it has a free tier and doesn’t require a credit card). You can adapt this to React or another framework if you prefer.

📝 Starter Code Example
html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Interview Question Generator</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    input, button { padding: 8px; margin-top: 10px; }
    #questions { margin-top: 20px; }
    .loading { color: gray; font-style: italic; }
  </style>
</head>
<body>
  <h2>AI-Powered Interview Question Generator</h2>
  <form id="jobForm">
    <label for="jobTitle">Enter Job Title:</label><br>
    <input type="text" id="jobTitle" placeholder="Customer Success Manager" required>
    <br>
    <button type="submit">Generate Questions</button>
  </form>

  <div id="status"></div>
  <div id="questions"></div>

  <script>
    const API_KEY = "YOUR_GEMINI_API_KEY"; // Replace with your Gemini API key
    const ENDPOINT = "https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=" + API_KEY;

    document.getElementById("jobForm").addEventListener("submit", async (e) => {
      e.preventDefault();
      const jobTitle = document.getElementById("jobTitle").value.trim();
      const status = document.getElementById("status");
      const questionsDiv = document.getElementById("questions");

      status.textContent = "Loading interview questions...";
      status.className = "loading";
      questionsDiv.innerHTML = "";

      try {
        const prompt = `Generate 3 thoughtful interview questions for a ${jobTitle}. 
                        Focus on role-specific skills, mindset, and problem-solving.`;

        const response = await fetch(ENDPOINT, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            contents: [{ parts: [{ text: prompt }] }]
          })
        });

        const data = await response.json();
        const text = data.candidates[0].content.parts[0].text;

        status.textContent = "";
        questionsDiv.innerHTML = "<h3>Suggested Interview Questions:</h3><p>" + text.replace(/\n/g, "<br>") + "</p>";
      } catch (error) {
        status.textContent = "Error fetching questions. Please try again.";
        console.error(error);
      }
    });
  </script>
</body>
</html>
🔑 Key Decisions
Provider & Model: Google Gemini (gemini-pro) — free tier, easy setup, good for text generation.

Prompt: Simple, role-specific, instructs the model to generate thoughtful questions.

UI: Minimal, readable, with a loading state.

Error Handling: Basic try/catch with user feedback.

Hosting: You can deploy this easily on Netlify, Vercel, or GitHub Pages.

🎥 Loom Video Checklist
When recording your Loom:

Introduce yourself briefly.

Show the app working (enter “Customer Success Manager” → see questions).

Walk through the code:

Why Gemini?

How the prompt is structured.

Error handling & loading state.

Answer reflection questions:

Improvement if more time (e.g., better UI, caching, role-specific fine-tuning).

Philosophy (build simple, iterate fast, clarity first).

Collaboration style (clear communication, async-friendly).

Problem-solving approach (break down issues, use docs, leverage AI tools).

Would you like me to also show you a React version of this (with hooks and state management), so you can pick whichever style you’re more comfortable with for your submission?

Yes
Here’s a React version of the technical screen assignment. It uses hooks for state management and keeps things simple but structured. This will give you a more modern frontend approach compared to the vanilla JS example.

⚛️ React Starter Code (with Gemini API)
jsx
import React, { useState } from "react";

function App() {
  const [jobTitle, setJobTitle] = useState("");
  const [questions, setQuestions] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  const API_KEY = "YOUR_GEMINI_API_KEY"; // Replace with your Gemini API key
  const ENDPOINT = `https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=${API_KEY}`;

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError("");
    setQuestions([]);

    try {
      const prompt = `Generate 3 thoughtful interview questions for a ${jobTitle}. 
                      Focus on role-specific skills, mindset, and problem-solving.`;

      const response = await fetch(ENDPOINT, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [{ parts: [{ text: prompt }] }]
        })
      });

      const data = await response.json();
      const text = data.candidates[0].content.parts[0].text;

      // Split into individual questions if possible
      const parsedQuestions = text.split("\n").filter((q) => q.trim() !== "");
      setQuestions(parsedQuestions);
    } catch (err) {
      setError("Error fetching questions. Please try again.");
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div style={{ fontFamily: "Arial, sans-serif", margin: "40px" }}>
      <h2>AI-Powered Interview Question Generator</h2>
      <form onSubmit={handleSubmit}>
        <label htmlFor="jobTitle">Enter Job Title:</label><br />
        <input
          type="text"
          id="jobTitle"
          value={jobTitle}
          onChange={(e) => setJobTitle(e.target.value)}
          placeholder="Customer Success Manager"
          required
          style={{ padding: "8px", marginTop: "10px" }}
        />
        <br />
        <button type="submit" style={{ padding: "8px", marginTop: "10px" }}>
          Generate Questions
        </button>
      </form>

      {loading && <p style={{ color: "gray", fontStyle: "italic" }}>Loading interview questions...</p>}
      {error && <p style={{ color: "red" }}>{error}</p>}

      {questions.length > 0 && (
        <div style={{ marginTop: "20px" }}>
          <h3>Suggested Interview Questions:</h3>
          <ul>
            {questions.map((q, index) => (
              <li key={index}>{q}</li>
            ))}
          </ul>
        </div>
      )}
    </div>
  );
}

export default App;
🔑 Key Notes
State Management: useState for job title, questions, loading, and error.

API Call: Same Gemini endpoint, wrapped in fetch.

UI: Clean, readable, with loading/error states.

Parsing: Splits the AI response into individual questions for better display.

Hosting: Deploy easily on Vercel or Netlify (React-friendly).

🎥 Loom Video Flow
Show the React app running locally (or deployed).

Type in “Customer Success Manager” → see questions generated.

Walk through the code:

Why React (state management, scalability).

Why Gemini (gemini-pro model, free tier).

Prompt design.

Error handling + loading state.

Answer reflection questions (improvements, philosophy, collaboration, problem-solving).

👉 If you want, I can also sketch out a GitHub repo structure + deployment steps (Netlify/Vercel) so you’ll have a complete submission package ready. Would you like me to do that?

Yes, I choose the React version of the technical screen assignment
Great choice — React is a strong fit for this assignment. Let’s put together a complete submission package so you’re ready to go from coding → hosting → submitting.

📂 GitHub Repo Structure
Here’s a simple structure you can use:

Code
interview-question-generator/
├── public/
│   └── index.html
├── src/
│   ├── App.js
│   ├── index.js
│   └── styles.css   (optional, if you want separate styling)
├── package.json
├── README.md
README.md (important for reviewers)
Include:

Project overview: “Simple React app that generates 3 interview questions for a given job title using Google Gemini API.”

Tech stack: React, Gemini API.

Setup instructions:

bash
git clone <repo-url>
cd interview-question-generator
npm install
npm start
Deployment link: Your live hosted URL (Netlify/Vercel).

Notes: Mention that you used AI tools for code generation/debugging.

🚀 Deployment Steps
Option 1: Vercel
Push your repo to GitHub.

Go to vercel.com.

Import your repo → select React framework.

Add your Gemini API key as an environment variable (REACT_APP_GEMINI_KEY).

Deploy → you’ll get a live URL like https://interview-questions.vercel.app.

Option 2: Netlify
Push repo to GitHub.

Go to netlify.com.

Connect repo → select React build (npm run build).

Add environment variable (REACT_APP_GEMINI_KEY).

Deploy → get live URL like https://interview-questions.netlify.app.

🔑 Submission Checklist
✅ GitHub Repo Link (with README and code).

✅ Live Hosted URL (Netlify/Vercel).

✅ Loom Video (4–7 min):

Intro yourself.

Show app working.

Walk through code (provider/model choice, prompt).

Answer reflection questions:

One improvement if more time.

Philosophy around building.

Collaboration style.

Problem-solving approach.

🧭 Next Step
Since you’ve chosen React, I recommend:

Add your Gemini API key as an environment variable (REACT_APP_GEMINI_KEY) instead of hardcoding it.

Update the fetch call in App.js to use process.env.REACT_APP_GEMINI_KEY.

That way, your repo stays clean and secure.

👉 Do you want me to refactor the React code I gave earlier to use environment variables (REACT_APP_GEMINI_KEY) so it’s production-ready for deployment?

Yes
Here’s the refactored React code using environment variables for your Gemini API key. This makes it secure and deployment‑ready for Netlify or Vercel.

⚛️ React Code with Environment Variable
jsx
import React, { useState } from "react";

function App() {
  const [jobTitle, setJobTitle] = useState("");
  const [questions, setQuestions] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  // Use environment variable instead of hardcoding
  const API_KEY = process.env.REACT_APP_GEMINI_KEY;
  const ENDPOINT = `https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=${API_KEY}`;

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError("");
    setQuestions([]);

    try {
      const prompt = `Generate 3 thoughtful interview questions for a ${jobTitle}. 
                      Focus on role-specific skills, mindset, and problem-solving.`;

      const response = await fetch(ENDPOINT, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          contents: [{ parts: [{ text: prompt }] }]
        })
      });

      const data = await response.json();
      const text = data.candidates[0].content.parts[0].text;

      const parsedQuestions = text.split("\n").filter((q) => q.trim() !== "");
      setQuestions(parsedQuestions);
    } catch (err) {
      setError("Error fetching questions. Please try again.");
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div style={{ fontFamily: "Arial, sans-serif", margin: "40px" }}>
      <h2>AI-Powered Interview Question Generator</h2>
      <form onSubmit={handleSubmit}>
        <label htmlFor="jobTitle">Enter Job Title:</label><br />
        <input
          type="text"
          id="jobTitle"
          value={jobTitle}
          onChange={(e) => setJobTitle(e.target.value)}
          placeholder="Customer Success Manager"
          required
          style={{ padding: "8px", marginTop: "10px" }}
        />
        <br />
        <button type="submit" style={{ padding: "8px", marginTop: "10px" }}>
          Generate Questions
        </button>
      </form>

      {loading && <p style={{ color: "gray", fontStyle: "italic" }}>Loading interview questions...</p>}
      {error && <p style={{ color: "red" }}>{error}</p>}

      {questions.length > 0 && (
        <div style={{ marginTop: "20px" }}>
          <h3>Suggested Interview Questions:</h3>
          <ul>
            {questions.map((q, index) => (
              <li key={index}>{q}</li>
            ))}
          </ul>
        </div>
      )}
    </div>
  );
}

export default App;
🔧 Setup Instructions
In your project root, create a .env file:

Code
REACT_APP_GEMINI_KEY=your_api_key_here
Add .env to .gitignore so you don’t commit your key.

In Netlify or Vercel:

Go to Project Settings → Environment Variables.

Add REACT_APP_GEMINI_KEY with your Gemini API key.

Redeploy.

📂 GitHub README Notes
Mention that the API key is stored in .env and must be set as REACT_APP_GEMINI_KEY.

Include deployment instructions for Netlify/Vercel.

Provide your live URL and Loom video link.

👉 With this refactor, your submission will look professional and secure. Do you want me to also draft a sample README.md file that you can drop straight into your repo?

Yes
Here’s a sample README.md you can drop straight into your GitHub repo for the React version of your technical screen assignment. It’s structured to be clear, professional, and reviewer‑friendly.

📄 Sample README.md
markdown
# AI-Powered Interview Question Generator

A simple React application that generates 3 thoughtful interview questions for any given job title using the **Google Gemini API**.  
This project was built as part of a technical screen for a Founding Engineer role.

---

## 🚀 Features
- Enter any job title (e.g., "Customer Success Manager").
- Calls the Gemini `gemini-pro` model to generate tailored interview questions.
- Clean, minimal UI with loading and error states.
- Secure API key handling via environment variables.

---

## 🛠️ Tech Stack
- React (Create React App)
- Google Gemini API (`gemini-pro` model)
- Hosted on Netlify/Vercel

---

## 📂 Project Structure
interview-question-generator/
├── public/
│   └── index.html
├── src/
│   ├── App.js
│   ├── index.js
│   └── styles.css   (optional)
├── package.json
├── README.md


---

## ⚙️ Setup Instructions

1. Clone the repository:
   ```bash
   git clone <your-repo-url>
   cd interview-question-generator

2. Install dependencies:

npm install

3. Create a .env file in the project root:

REACT_APP_GEMINI_KEY=your_api_key_here

4. Start the development server:

npm start

